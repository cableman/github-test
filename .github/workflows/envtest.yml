on:
  push:
    branches:
      - '*'

jobs:
  cleanUp:
    runs-on: ubuntu-lastest
    strategy:
      matrix:
        site: [SITE1, SITE2]
    steps:
      - name: Test (${{ matrix.site }})
        run: echo ${{ secrets[env.SITE_PATH] }}
        env:
          SITE_PATH: APP_TEST_${{ martix.site }}
